# Project Configuration

Use this folder for project-local configuration such as:

- approved non-production domain allowlists
- safety limits
- synthetic-data generation settings
- component strategy configuration
- diagnostic schemas
- lint and formatting rules
- environment variable examples

Do not store credentials, access tokens, secrets, production-sensitive values, or personal information here.

Operational instructions belong in `../AGENTS.md`. Tool-readable configuration belongs here.

